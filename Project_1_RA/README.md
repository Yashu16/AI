# Research Assistant

An AI-Powered research agent that searches the web and returns structured summaries on any topic.

## what it does:

- User asks a question
- Agent uses a Tavily to find relevant information on the web
- Agent summarizes the information and provides a structured response to the user using gemini
- Returns structured output: summary, key facts, sources and confidence score

## Tech stack

- Python 3.11+
- Tavily API key
- A Gemini API key 
- google-genai, tavily-python, python-dotenv

### Setup (Windows PowerShell)

```powershell
cd "c:\Yaswanth files\AI"
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -r .\Project_1_RA\requirements.txt
```

### Configure API Key

Create a `.env` file in the `Project_1_RA` directory with the following content:

TAVILY_API_KEY=your_api_key_here
GEMINI_API_KEY=your_api_key_here

## How to Run
1. clone the repo and navigate to the `Project_1_RA` directory
2. Install the dependencies and set up the API keys as described above
3. Run the agent with `python agent.py`
4. Type your research question and press Enter

Type `quit` to exit.

## How it works 
- excerpt from my explanation from below:
Uses a ReAct (Reasoning + Acting) loop - gemini reasons about the query, decides to call web search, gets results, reasons again, and finally generates a structured answer.

## Example output
```
    You: I want to know more about LLM in financial markets

    Searching: LLM in financial markets


    --- Research Results ---

    Summary: Large Language Models (LLMs) are increasingly being integrated into financial markets to perform complex analyses and improve decision-making. Applications include stock price forecasting, sentiment analysis, portfolio management, and algorithmic trading. LLMs can process unstructured textual data alongside structured financial indicators to generate market signals and enhance prediction models.

    Key Facts:
    - LLMs are used in finance for tasks like stock forecasting, sentiment analysis, and algorithmic trading.
    - Research shows that LLMs can outperform traditional methods in predicting stock market returns.
    - LLMs can be integrated with techniques like Retrieval-Augmented Generation (RAG) to improve accuracy, though this may increase costs and latency.
    - GPT-5 (as of August 2025) leads in financial reasoning tasks with 88.23% accuracy.

    Sources: reddit.com, aimultiple.com, ncbi.nlm.nih.gov, arxiv.org, llm-guide.com
    Confidence: high
```

## Notes

- Use `python -m pip` to ensure packages install into the active environment.
- The `.venv` folder is ignored by Git and should not be committed.


- Below is explanations of the code and the reasoning behind it, as well as some insights into how the agent works and how we structured the code.

 ## Explanations
 #### Search tool does:
 LLM requires tools to be defined in a specific format. This is an example of how to define a tool for web search. 
 The tool is defined with a function declaration that specifies the name, description, and parameters of the tool. 
 The LLM can then call this tool when it needs to perform a web search. 
 The response from the tool can be fed back into the conversation history for the LLM to use in generating its next response.
 So, function declaration -> name, desc, params(type, properties(query, type, desc), required(query))
 Parameters are filled by LLM when the user asks.

 #### Candidate[0] means the best answer among the candidates generated by Gemini. Gemini generates multiple candidates for each response, and we are taking the first one (index 0) as the best one to display to the user.


 And why do we have two while loops? Outer loop is for conversation and inner loop is for tool calling, and search-retreive and call itself again. Say we are searching: "compare DC vs Japan cherry blossoms" - Search for DC cherry blossoms -> loop back, search for Japan Cherry blossoms -> loop back and now compares them, calls itself again to get the final answer. 

 This is called ReAct (Reasoning + Acting) where the agent can reason about what tool to use and then act by calling the tool, and then use the results to reason further and act again until it reaches a final answer.

 #### Day_4: 
 Instead of letting gemini return raw final answer, we can let it return a JSON with summary and key facts, and then we can parse that JSON and display it in a more structured way to the user. This is more robust than relying on the model to format its response in a specific way, and allows us to handle the response more effectively.

 So far the agent talked back in plain conversational text. That's fine for a chatbot, but if we ever want to:

- Display results in a UI
- Save research to a file
- Feed output into another system

 we need structured output. Instead of Gemini rambling in paragraphs, you tell it to always return clean JSON in a shape you define.